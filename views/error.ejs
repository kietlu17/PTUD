<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lỗi Hệ thống</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .error-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-container bg-white text-center">
            <h1 class="text-danger">❌ Lỗi Truy Cập</h1>
            <hr>
            <p class="lead">Đã xảy ra lỗi khi xử lý yêu cầu của bạn.</p>
            
            <% if (typeof message !== 'undefined') { %>
                <div class="alert alert-warning mx-auto mt-3" style="max-width: 80%;">
                    <strong>Chi tiết lỗi:</strong> <%= message %>
                </div>
            <% } %>

            <a href="/" class="btn btn-primary mt-3">Quay về Trang chủ</a>
        </div>
    </div>
</body>
</html>