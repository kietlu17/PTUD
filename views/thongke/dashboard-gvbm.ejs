<%- include('../partials/header', { title: 'Thống Kê Bộ Môn' }) %>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<div class="container-fluid">
  <div class="row">
    <%- include('../partials/nav-giaovien') %>

    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 py-4">
        <h3 class="fw-bold text-info mb-4">📊 THỐNG KÊ BỘ MÔN: <%= monHoc ? monHoc.TenMon : 'Chưa cập nhật' %></h3>
        <% if (typeof canSwitchToGVCN !== 'undefined' && canSwitchToGVCN) { %>
                <a href="/giaovien/thongke" class="btn btn-outline-success fw-bold shadow-sm">
                    <i class="bi bi-arrow-repeat me-2"></i> Quay lại Lớp Chủ nhiệm
                </a>
        <% } %>
        <div class="card shadow-sm mb-4">
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-3">
                        <label class="fw-bold">Năm Học:</label>
                        <select id="filterNamHoc" class="form-select">
                            <option value="<%= defaultNamHoc %>"><%= defaultNamHoc %></option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label class="fw-bold">Học Kỳ:</label>
                        <select id="filterHocKy" class="form-select">
                            <option value="1">Học kỳ 1</option>
                            <option value="2">Học kỳ 2</option>
                        </select>
                    </div>
                    
                    <div class="col-md-3">
                        <label class="fw-bold">Chọn Lớp Dạy:</label>
                        <select id="filterLop" class="form-select">
                            <% dsLop.forEach(l => { %>
                                <option value="<%= l.id %>"><%= l.TenLop %></option>
                            <% }) %>
                        </select>
                    </div>

                    <div class="col-md-3">
                        <label class="fw-bold">Môn:</label>
                        <select id="filterMon" class="form-select bg-light" disabled>
                            <% if(monHoc) { %>
                                <option value="<%= monHoc.id %>" selected><%= monHoc.TenMon %></option>
                            <% } else { %>
                                <option value="">Chưa có chuyên môn</option>
                            <% } %>
                        </select>
                    </div>
                </div>
                <div class="mt-3 text-end">
                     <button class="btn btn-info text-white px-4" onclick="loadData()">Xem Thống Kê</button>
                </div>
            </div>
        </div>

        <%- include('./partials/charts_and_table') %>
    </main>
  </div>
</div>

<%- include('./partials/script_logic') %>