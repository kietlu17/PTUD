<%- include('../partials/header', { title: 'Thống Kê Lớp Chủ Nhiệm' }) %>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<div class="container-fluid">
  <div class="row">
    <%- include('../partials/nav-giaovien') %>

    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 py-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h3 class="fw-bold text-success">📊 THỐNG KÊ LỚP CHỦ NHIỆM: <%= lop.TenLop %></h3>
            <span class="badge bg-secondary fs-6">Sĩ số: <%= siSo %></span>
        </div>
        <% if (typeof canSwitchToGVBM !== 'undefined' && canSwitchToGVBM) { %>
                <a href="/giaovien/thongke?view=gvbm" class="btn btn-outline-primary fw-bold shadow-sm">
                    <i class="bi bi-arrow-repeat me-2"></i> Chuyển sang Thống kê Bộ môn
                </a>
        <% } %>
        <div class="card shadow-sm mb-4">
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-3">
                        <label class="fw-bold">Năm Học:</label>
                        <select id="filterNamHoc" class="form-select">
                            <option value="<%= defaultNamHoc %>"><%= defaultNamHoc %></option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label class="fw-bold">Học Kỳ:</label>
                        <select id="filterHocKy" class="form-select">
                            <option value="1">Học kỳ 1</option>
                            <option value="2">Học kỳ 2</option>
                        </select>
                    </div>
                    
                    <div class="col-md-3">
                        <label class="fw-bold">Lớp:</label>
                        <select id="filterLop" class="form-select bg-light" disabled>
                            <option value="<%= lop.id %>" selected><%= lop.TenLop %></option>
                        </select>
                    </div>

                    <div class="col-md-3">
                        <label class="fw-bold">Môn Học:</label>
                        <select id="filterMon" class="form-select">
                            <option value="">-- Tất cả các môn --</option>
                            <% dsMon.forEach(m => { %>
                                <option value="<%= m.id %>"><%= m.TenMon %></option>
                            <% }) %>
                        </select>
                    </div>
                </div>
                <div class="mt-3 text-end">
                     <button class="btn btn-success px-4" onclick="loadData()">Xem Thống Kê</button>
                </div>
            </div>
        </div>

        <%- include('./partials/charts_and_table') %>
    </main>
  </div>
</div>

<%- include('./partials/script_logic') %>